# STEPN 収支管理ツール 引き継ぎドキュメント v9

**作成日：2026/08/14　次チャットへの即時引き継ぎ用**
**前版：v8（同日）／ v7（2026/06/25）**

> **v9 の位置づけ**
> v8 は v3（5/6）とコードだけを見て書いたため、v4〜v7 の決定事項が抜けていた。
> 本 v9 は、Claudeプロジェクトの全コンテキスト11本を統合したアーカイブ
> [`docs/handoff/STEPN_全コンテキスト統合アーカイブ_20260814.md`](./docs/handoff/STEPN_全コンテキスト統合アーカイブ_20260814.md)
> を突き合わせて作成した**初めての完全版**である。

---

## 🔧 プロジェクト基本情報

| 項目 | 内容 |
|---|---|
| **目的** | STEPNのスクショから ML Kit OCR で自動解析 → 収支記録・管理アプリ（確定申告用CSV出力まで） |
| **開発者** | のっち × Claude |
| **技術** | ML Kit OCR / React Native 0.81.5 / Expo SDK 54 / AsyncStorage / CSV出力 |
| **チェーン** | SOL / BNB / POL（ETHは非アクティブにつき削除済み） |
| **開発環境** | Windows 10 / Node.js v24.15.0 / npm v11.12.1 |
| **フォルダ** | `C:\Users\nochittking\stepn-tracker` |
| **Expoアカウント** | nochittking（expo.dev） |
| **実機** | Androidメイン（Development Build済み）／ iOSは年会費問題でDev Buildなし |
| **リポジトリ** | https://github.com/nochittking/STEPN-Tracker （public） |
| **Androidビルド** | `set EAS_NO_VCS=1 && npx eas build --profile development --platform android` |

---

## 🚨 絶対に守るべき鉄のルール（毎回必読）

### コストルール（最重要・永久不変）

- ✅ OCRは **ML Kit**（`@react-native-ml-kit/text-recognition`）一択。オフライン・無料
- ✅ ピクセル解析は **expo-image-manipulator**（無料）
- ✅ 通常の通信費・無料外部API（CoinGecko等）は問題なし
- ❌ **画像解析の「1枚あたり〇円」従量課金API（Claude Vision等）は絶対NG**
- ❌ expo-crypto のようなネイティブモジュールは Dev Build 再ビルドが必要なため要注意

### 開発の進め方ルール

1. のっちの投げかけに対して → **まず原因分析**を提示する
2. **解決案を複数提示** → メリット・デメリットをセットで
3. 解決案の**シミュレート検証を2回以上**繰り返し、確実性90%以上を確認
4. 「**進めてください**」の返答を待つ
5. **承認後に初めてコードに触る**（勝手にコード修正に走らない）

### 会話ルール

- 常に**関西弁**で話す
- STEPNの専門用語はそのまま使う（GST / GMT / EN / MB / HP / Durability 等）
- わからないことは都度説明する

### 動作確認手順の伝え方（毎回この形式）

```
① コマンドプロンプトを開く
② cd C:\Users\nochittking\stepn-tracker
③ npx expo start（または起動中なら r キーでリロード）
④ QRコードをAndroidのDevelopment Buildアプリでスキャン
⑤ 動作確認
```

### STEPNドメイン知識（混同禁止）

| 項目 | 内容 |
|---|---|
| **靴ID** | `#XXXXXXXXX` = 通常靴 ／ `GXXXXXXXXX` = Genesis靴。**GSTと絶対に混同しない** |
| **ジェムLv** | `+2` `+72` はバフ値。Lvは形状で判断（**OCR不可**） |
| **出金手数料** | **変動**（チェーン・トークン種別で異なる）。「固定」と絶対に言わない |
| **OCR時刻** | ステータスバーの時刻は**使わない**。取込時刻（`created_at`）を使う |
| **MB排出ジェムLv** | 左から順に **Lv1/Lv2/Lv3固定**。ミンスクはLvカウントに含めない |
| **MBジェムLv上限** | MBからは **Lv4まで**しか排出されない |

---

## 🗂 カテゴリ体系（全22種）★確定・v9で決着★

### 正典は `MANUAL_GROUPS`（8大分類 → 22カテゴリ）

```javascript
const MANUAL_GROUPS = {
  '💚 収入':      ['move_result', 'mb_result', 'marketplace_sell', 'spending_deposit'],
  '🔧 修復・強化': ['repair_hp', 'repair_durability', 'level_up', 'socket_unlock', 'shoe_enhance'],
  '💎 ジェム':     ['gem_upgrade_success', 'gem_upgrade_fail', 'gem_upgrade_confirm'],
  '👟 ミント':     ['shoe_mint_cost', 'shoe_mint_result'],
  '📦 MB':        ['mystery_box_open'],
  '🏪 マーケット':  ['marketplace_buy', 'marketplace_listing'],
  '💸 送金':       ['spending_withdraw'],
  '⚙️ その他':     ['success_rate_increment', 'point_redistribution', 'vip_membership', 'home'],
};
```

**合計 4+5+3+2+1+2+1+4 = 22種**

### 🔴 v8 の記述を訂正する

v8 では「カテゴリ体系に食い違いがあり、どちらが現行仕様か実装を見て確定させる作業が必要」と書いた。
また作業中に一時「正しくは24種で、i18n に3種足りない」と述べたが、**これは誤りだった**。

根拠としていた `全体仕様書_v5まとめ`（2026/05/28）は、その後の **v6（6/16）・v7（6/25）で上書きされている**。

**確定した事実：**

| 対象 | 判定 |
|---|---|
| `src/i18n/i18n.js` の `cat_` 辞書（22種） | ✅ **正しい。`MANUAL_GROUPS` と完全一致**。不足なし |
| `src/screens/ImportScreen_constants.js`（23種） | ✅ 正しい（22種 + `unknown` フォールバック） |
| `src/constants/index.js`（25種） | ⚠️ **古い**。廃止カテゴリが残存 |
| `src/components/index.js`（25種） | ⚠️ `constants/index.js` と完全同一のデッドコピー |
| `src/screens/CsvExportModal.js`（23種） | ✅ 正しい（22種 + `unknown`） |

### 廃止されたカテゴリ（`constants/index.js` に残る残骸）

`achievement` / `leaderboard` / `profile_settings` / `energy_refill` / `gem_combine` / `attribute_up`

→ v5以前の設計には存在したが、**v6以降の22種には含まれない**。
→ `constants/index.js` からの削除が正しい方向。i18n に追加してはいけない。

### ⚠️ 「CsvExportModal に11カテゴリ欠落」は誤りだった（v9初版の訂正）

v9初版には「CsvExportModal.js は11種しかなく、CSV出力時に生のカテゴリIDが
そのまま吐かれる実害がある」と書いたが、**これは完全な誤りである。**

原因は、`CATEGORY_LABELS` が**1行に2エントリずつ**書かれているのを、
行数で数えてしまったこと（11行 × 2 = 22エントリ + `unknown` = 23）。

```javascript
// CsvExportModal.js — 1行に2個ずつ書かれている
move_result: 'ムーブ結果', level_up: 'レベルアップ',
repair_hp: 'HP修復', repair_durability: 'Durability修復',
```

パースして数えた実測値：

| ファイル | エントリ数 |
|---|---|
| `CsvExportModal.js` | **23**（22種 + `unknown`）✅ |
| `ImportScreen_constants.js` | 23 ✅ |
| `i18n.js` の `cat_` | 23 ✅ |
| `constants/index.js` | 25 ⚠️（廃止分が残存） |

`CsvExportModal` と `i18n` の差分は**ゼロ**。CSV出力にカテゴリ欠落の実害は存在しない。

> **教訓：** 辞書やオブジェクトのエントリ数を `grep -c` などの行単位で数えない。
> 必ずキーを正規表現で抽出して数えること。この誤りは v8→v9 で2回発生している
> （1回目はカテゴリ総数を24種と誤認、2回目が本件）。

---

## 📋 現在の進捗状況（2026/08/14 実測）

### ✅ 完成済み機能（v7時点で完成、以降変更なし）

| 機能 | 備考 |
|---|---|
| スクショ取込（OCR解析） | ML Kit / カテゴリ自動判定 / サムネイル表示 |
| チェーン自動判定 | ピクセル解析で SOL/BNB/POL 推定（オーバーレイ対応済み） |
| 手動入力 | 8大分類 → 22カテゴリの2段階選択 |
| 記録一覧 | ALL/収入/支出/情報/売却中タブ / チェーンフィルタ / 複数選択削除 |
| レコード詳細・編集 | タップで詳細表示・編集・保存 / サムネイル拡大モーダル |
| ホーム画面 | チェーン選択 / Spending残高 / 期間切替（今月/今年/全期間/月指定） |
| CSV出力 | 28列 / BOM付きUTF-8 / 矢印月ピッカー / Android共有シート |
| 設定画面 | 初期残高（チェーン別）/ 残高照合 |
| 使い方ガイド | 全機能の取扱説明書 |
| 重複検出（FNV-1a） | expo-crypto不要・純JS・先頭5KB+末尾5KB |
| 類似レコード検出 | 同カテゴリ・同金額・**3時間以内**（24h→3hに短縮済み） |
| サムネイル保存 | 取込時300px幅に縮小・`thumbnails/` に保存 |
| MB結果画面の自動検出 | ピクセル解析（輝度サンプリング12点） |
| MB開封内容の手動確認UI | 色4択・Lv自動・ミンスクレアリティ5択 |

### 🔄 進行中：多言語化（i18n）★v8以降の新フェーズ★

v7 の「🔲 未実装」にあった **言語切替（日英）** が、現在進行中のフェーズ。

**基盤：完成済み** — `src/i18n/i18n.js`（540行・v1.0.0）

- ネイティブライブラリ不使用（**Dev Build のリビルド不要**という設計判断）
- React Context で言語を全画面共有 → トグルで即時反映
- AsyncStorage（キー `settings_lang`）に保存 → 再起動しても復元
- `App.js` 全体を `<LanguageProvider>` で包む（v3.1.0で対応済み）

**辞書の2タイプ：**

| タイプ | 形式 | 用途 |
|---|---|---|
| A（文字列） | `key → { ja: "文字列", en: "string" }` | 固定文言 |
| B（関数） | `key → { ja: (args)=>\`...\`, en: (args)=>\`...\` }` | 変数が混じる文言 |

**`t()` の安全設計：** 関数なら実行／文字列ならそのまま返す（型判定）。キーが無ければ**キー名をそのまま返す**（クラッシュ防止）。

**公開API：** `LanguageProvider`（L489） / **`useI18n`**（L538）
> ⚠️ フック名は **`useI18n`**。`useLang` ではない。

**画面別の対応状況（`useI18n` の import 有無で実測）：**

| 画面 | 状態 |
|---|---|
| `HomeScreen.js` | ✅ 対応済み |
| `ImportScreen.js` | ✅ 対応済み |
| `ImportScreen_item.js` | ✅ 対応済み |
| `RecordListScreen.js` | ✅ 対応済み |
| `SettingsScreen.js` | ✅ 対応済み（+ 言語切替セクションを実装） |
| `RecordDetail.js` | ✅ 対応済み（詳細フィールド69項目を含む） |
| `ManualInput.js` | ✅ 対応済み |
| `CsvExportModal.js` | ✅ 対応済み（**画面UIのみ**。CSV本文は日本語固定） |
| `SettingsScreen_guide.js` | ✅ 対応済み（全9セクション） |
| ~~`Placeholders.js`~~ | 🗑️ **削除済み**（全画面実装済みで到達不能なデッドコードだった） |

### 🔄 進行中：テーマ（ダーク/ライト）★i18n完了後の新フェーズ★

**基盤：完成済み** — `src/theme/theme.js`（v1.0.0）

- i18n と同じ React Context パターン。**ネイティブ不使用＝Dev Build 再ビルド不要**
- AsyncStorage（キー `settings_theme`）に保存 → 再起動しても復元
- `App.js` を `<ThemeProvider>` で包む（`LanguageProvider` の外側）
- 公開API：`ThemeProvider` / **`useTheme`** → `{ colors, theme, setTheme, chainColors }`

**画面側の書き方（これを守ること）：**

```javascript
// StyleSheet.create はモジュールスコープで1回しか走らないため、
// 直書きのままではテーマ切替に追従できない。必ず関数にする。
const makeStyles = (c) => StyleSheet.create({
  container: { backgroundColor: c.bg },
});

export default function Screen() {
  const { colors } = useTheme();
  const s = useMemo(() => makeStyles(colors), [colors]);
}
```

**ライトテーマの配色（重要）：**

ダークの色をそのまま白背景に載せると読めない。実測コントラスト比：

| 色 | 白背景での比 | 判定 |
|---|---|---|
| `#00ff88`（ダークの収入色） | **1.34:1** | 🔴 不可 |
| `#ffaa00`（ダークの警告色） | **1.91:1** | 🔴 不可 |
| `#00703a`（ライトの収入色） | 6.22:1 | ✅ |
| `#c62828`（ライトの支出色） | 5.62:1 | ✅ |

→ ライトテーマでは意味を持つ色を濃く調整済み。**全トークンが 4.5:1 以上を確保**。
→ ただし **SOL/BNB/POL のチェーンカラーは公式ブランド色なので両テーマ据え置き**。

**画面別の対応状況：**

| 画面 | 状態 |
|---|---|
| `App.js`（ヘッダー・ステータスバー） | ✅ 対応済み |
| `SettingsScreen.js` | ✅ 対応済み（+ テーマ切替UIを実装） |
| `HomeScreen.js` | 🔲 未対応（色59箇所） |
| `RecordListScreen.js` | 🔲 未対応（色80箇所） |
| `ImportScreen_styles.js` | 🔲 未対応（色79箇所） |
| `ImportScreen_item.js` | 🔲 未対応（色55箇所） |
| `ManualInput.js` | 🔲 未対応（色32箇所） |
| `RecordDetail.js` / `RecordDetail_styles.js` | 🔲 未対応（色49箇所） |
| `CsvExportModal.js` | 🔲 未対応（色18箇所） |
| `SettingsScreen_guide.js` | 🔲 未対応（色5箇所） |
| `ImportScreen_constants.js` | 🔲 未対応（色10箇所） |

> 全体では**延べ495箇所**のハードコード色があった。`constants/index.js` の `COLORS`
>（25トークン）は語彙としては良かったが、実参照はわずか4回でほぼ使われていなかった。

---

### 🔲 未実装・将来対応（v7から継続）

| 機能 | 優先度 | 備考 |
|---|---|---|
| MBジェム色の自動判定精度向上 | 中 | slot 1/2 の色判定が不安定（グロー枠の影響）→ 現状は手動選択でカバー |
| 文字サイズ3段階 | 中 | 全画面 fontSize 改修が必要 |
| CSV円換算レート（CoinGecko API） | 低 | 無料API利用可能 |
| MB結果の紐付けUI | 低 | `linkMbResult` は実装済み・UIが未完 |

---

## 🔬 重要な技術仕様（v7で確定）

### 1. FNV-1aハッシュ（重複検出）

```javascript
// expo-crypto を使わず純JSで実装（Dev Build再ビルド回避）
const fnv1aHash = (str) => {
  let h = 2166136261;
  for (let i = 0; i < str.length; i++) {
    h ^= str.charCodeAt(i);
    h = (h * 16777619) >>> 0;
  }
  return h.toString(16).padStart(8, '0');
};
// 先頭5KB+末尾5KBでハッシュ（衝突回避）
imageHash = fnv1aHash(base64.slice(0, 5000) + base64.slice(-5000));
```

### 2. サムネイル保存

```
取込時   : expo-image-manipulator で幅300pxに縮小 → JPEG 70%品質
保存先   : FileSystem.documentDirectory + 'thumbnails/' + thumbName
フィールド: thumbnail_uri（相対パス）・image_hash
削除時   : image_hash とサムネファイルも連動削除
容量目安 : 1枚約50KB → 毎日10枚×5年で約1GB以下
```

### 3. ピクセル解析（`VisionAnalyzer_pixel.js` v3.4.x）

**チェーン判定（`_classifyChainColor`）** — オーバーレイ越しの実測値ベース。**絶対値でなく色パターンで判定**：

```javascript
BNB: r > g && g > b && r - b > 12 && sat > 8   // 実測 RGB(81,78,63)
POL: b > g && r > g && sat > 10                 // 実測 RGB(51,39,73)
SOL: それ以外（グレー系）                          // 実測 RGB(72,83,79)
// クロップ座標: x=68-96%, y=3-11%（4x1リサイズ）
```

**MB結果検出（`detectMbResultByPixel`）：**

```javascript
// 12ポイントサンプリング: y=62%,68%,74% × x=15%,38%,62%,85%
// 輝度(bri) > 160 が3点以上 → mb_result
// ホーム画面はSTARTボタン（暗緑）+暗い背景 → bri低い → home
```

**MBスロット色判定（`_classifyMbSlot`）** — slot 0（左端）は安定、slot 1・2 はグロー影響で不安定。→ **取込確認画面でユーザーが色を手動選択するハイブリッド方式**で運用：

```javascript
efficiency: r > g && g > b && r - b > 30  // 黄/金
comfort:    r > b && r >= g && sat > 5    // 赤/オレンジ
resilience: b > g && b > r && b - g > 8   // 紫
luck:       g > r && b > r                // 水色
scroll:     sat <= 8                      // グレー（ミンスク）
```

### 4. その他の絶対仕様

```javascript
// 二重保存防止：useStateはバッチ更新で即時反映されないためuseRefを使う
const isSavingRef = useRef(false);

// expo-file-system は SDK54 では /legacy インポート必須
import * as FileSystem from 'expo-file-system/legacy';

// FlatList
_ukey → keyExtractor専用 ／ r.id → 選択・削除・全選択に使用

// 残高照合の戻り値は .calculated を使う
const gstBalance = calc.gst?.calculated ?? 0;

// Settings のキー名は initial_balances（アンダースコア）

// PNG解析はAndroidのグレースケールPNG対応のため
// 4x1リサイズ + _parsePngRow（Subフィルタ累積復元対応済み）
```

### 5. CSV出力の言語方針（★確定・変更不可）

**CSVファイルの中身は、アプリの言語設定にかかわらず常に日本語で固定する。**

| 対象 | 言語 |
|---|---|
| CSV出力モーダルの**画面UI** | アプリ言語に連動（`t()` 使用） |
| CSVの**28列ヘッダー**（`取込日時,カテゴリ,...`） | **常に日本語固定** |
| CSVの**カテゴリ名**（`ムーブ結果` 等・`CATEGORY_LABELS`） | **常に日本語固定** |
| **ファイル名**（`stepn_SOL_2026-08.csv`） | 常にASCII固定 |

**理由：** この機能の主目的が確定申告用の出力であるため。
言語を切り替えた前後で列名が変わると、**過去に出力したCSVと結合したときに
列がズレて集計が壊れる**。UIの言語設定が保存済みファイルの構造を変えてはいけない。

**実装上の禁止事項：**
`CsvExportModal.js` の以下3つに `t()` を通してはいけない。

```
buildCsv()        … 28列ヘッダーと行データ
CATEGORY_LABELS   … カテゴリ日本語名（23種）
buildFilename()   … ファイル名
```

### 6. チェーンカラー（確定）

| チェーン | カラー | 由来 |
|---|---|---|
| SOL | `#9FFB50` | STEPN Green（PANTONE 16-0163・STEPNxPantone公式） |
| BNB | `#F3BA2F` | BNB Chain Yellow（BNB公式） |
| POL | `#9063CD` | STEPN GO Purple（PANTONE 265・POL専用） |

---

## 🐛 解決済みバグ一覧（再発防止用）

| バグ | 原因 | 解決 |
|---|---|---|
| expo-crypto ネイティブエラー | Dev Buildに未組込 | FNV-1a純JS実装に置き換え |
| ハッシュ衝突（同じ画面が取込済み扱い） | 先頭10KBのみハッシュ化 | 先頭5KB+末尾5KBに変更 |
| 類似レコード頻発 | 24時間以内の同金額を全て検出 | 3時間以内に短縮 |
| チェーン全部SOL判定 | `_parsePng1x1` がグレースケールPNGを誤読 | 4x1リサイズ + `_parsePngRow` + カラータイプ対応 |
| Subフィルタ誤読 | PNG Subフィルタの累積復元が未実装 | Uint8Arrayで復元値を累積 |
| チェーン判定SOL固定 | 彩度<35でSOL判定（先にsatチェック） | **色パターン（R>G>B等）を先にチェック** |
| MB結果がhome判定 | `pako.inflate` にPNG全体を渡していた | 12点輝度サンプリングに変更 |
| row2空スロットでミンスク選択が出る | row2を無条件でscroll扱い | 輝度<80の場合はスキップ |
| 記録一覧で mb_result が「？不明」 | RecordListScreen独自の `CATEGORY_LABELS` に未定義 | 追加済み |
| `mb_reward_items` が `[object Object]` 表示 | 配列を `String()` 変換 | 絵文字付きフォーマット表示に変更 |
| 二重保存 | `useState` はバッチ更新で即時反映されない | `useRef` に変更 |

---

## ✅ 構文チェックの正しい方法（v7から更新）

> **⚠️ v7 までのルールを更新した項目（実測で検証済み）**
> v7 には「`node --input-type=module < ファイル名.js`」と書かれているが、
> **この方法は JSX ファイルに対して完全に無意味**である。以下は実測結果：
>
> ```
> 正常な SettingsScreen.js → 終了コード 1
> わざと壊したコピー       → 終了コード 1   ← 同じ！
> ```
>
> Node は JSX 構文そのものを解釈できないため、**ファイルの正否にかかわらず必ず
> 最初の JSX 行でエラーを出す**。つまり「エラーが出た＝壊れている」の判断ができない。
> しかも報告される行番号は実際の不具合箇所と無関係（正常ファイルでも 125行目の
> `<SafeAreaView>` で落ちる）。以下に置き換えること。

### ✅ 方法A：esbuild（推奨・一発）

```bash
npx --yes esbuild --loader:.js=jsx --log-level=warning --outfile=/dev/null src/screens/RecordDetail.js
```

> **⚠️ `--loader=jsx` ではなく `--loader:.js=jsx` と書くこと。**
> `--loader=jsx`（コロンなし）は stdin 専用のオプションで、ファイルを渡すと
> `"loader" without extension only applies when reading from stdin` というエラーになる。
> v9 初版はこれを間違えていたため修正した。

一括チェック：

```bash
for f in src/screens/*.js src/services/*.js src/i18n/*.js App.js; do
  npx --yes esbuild --loader:.js=jsx --log-level=warning --outfile=/dev/null "$f" || echo "❌ $f"
done
```

**検証済みの挙動：** 閉じタグを壊したファイルに対して
`Unexpected closing "ScrollView" tag does not match opening "Text" tag` と
**正確な不具合箇所を指摘**することを確認済み。

### ✅ 方法B：@babel/core + @babel/preset-react

```bash
npm i -D @babel/core @babel/preset-react
```

```bash
node -e "
const babel = require('@babel/core');
const fs = require('fs');
const file = process.argv[1];
try {
  babel.transformSync(fs.readFileSync(file, 'utf8'), {
    filename: file,
    presets: [['@babel/preset-react', { runtime: 'automatic' }]],
  });
  console.log('✅ OK:', file);
} catch (e) {
  console.error('❌ NG:', file, '\n', e.message);
  process.exit(1);
}
" src/screens/RecordDetail.js
```

**ポイント：実際にコンパイルを通すこと。** パースだけ・目視だけでは JSX の閉じ忘れやタグの対応ミスを検出できない。

---

## 📁 ファイル構成（24ファイル・約9,158行）

### ルート
| ファイル | 行数 | 内容 |
|---|---|---|
| `App.js` | 137 | ナビゲーション（8画面登録）+ LanguageProvider |
| `index.js` | 8 | エントリポイント |

### src/screens/（12ファイル）
| ファイル | 行数 | 内容 |
|---|---|---|
| `HomeScreen.js` | 453 | ホーム（期間切替4タブ） |
| `ImportScreen.js` | 586 | スクショ取込（FNV-1aハッシュ・サムネイル生成） |
| `ImportScreen_item.js` | 694 | ConfirmItem・SpecialFlow・MbSlotEditor・DonePhase |
| `ImportScreen_constants.js` | 75 | 定数・confLabel・CATEGORY_LABELS（22種+unknown） |
| `ImportScreen_styles.js` | 139 | StyleSheet |
| `RecordListScreen.js` | 606 | 記録一覧 |
| `RecordDetail.js` | 494 | レコード詳細・編集 |
| `RecordDetail_styles.js` | 65 | スタイル |
| `ManualInput.js` | 331 | 手動入力（8大分類→22カテゴリ） |
| `SettingsScreen.js` | 360 | 設定（初期残高・残高照合） |
| `SettingsScreen_guide.js` | 153 | 取扱説明書 |
| `CsvExportModal.js` | 357 | CSV出力モーダル |

### src/services/（5ファイル）
| ファイル | 行数 | 内容 |
|---|---|---|
| `VisionAnalyzer.js` | 176 | メイン・カテゴリ判定司令塔 |
| `VisionAnalyzer_parsers.js` | 797 | 全20カテゴリパーサー |
| `VisionAnalyzer_pixel.js` | 682 | ピクセル解析 |
| `StorageService.js` | 801 | CRUD・仮保存・修正履歴・重複検出 |
| `StorageService_query.js` | 421 | getRecords・集計・設定・残高照合 |

### その他
| ファイル | 行数 | 内容 |
|---|---|---|
| `src/i18n/i18n.js` | 540 | 多言語辞書 + LanguageProvider / useI18n |
| `src/constants/index.js` | 275 | 定数（※CATEGORY_LABELSは古い） |
| `src/components/index.js` | 275 | ⚠️ `constants/index.js` の完全同一デッドコピー |
| `src/screens/VisionAnalyzer_pixel.js` | 682 | ⚠️ `services/` 側と完全同一のデッドコピー |
| `src/screens/Placeholders.js` | 51 | プレースホルダー |

---

## 🧹 技術的負債（動作影響なし・整理候補）

| # | 内容 | 対応方針 |
|---|---|---|
| 1 | `src/components/index.js` がデッドコード | `constants/index.js` と完全同一（275行）。import している箇所が**0件**。削除候補 |
| 2 | `VisionAnalyzer_pixel.js` の二重配置 | `screens/` と `services/` に完全同一（682行）。使われているのは `services/` 側のみ。`screens/` 側は削除候補 |
| 3 | `CATEGORY_LABELS` 4箇所重複 | i18n の `cat_` 辞書に一本化して解消 |

→ **i18n完了後にまとめて refactor コミット**するのが安全（今やると翻訳作業と衝突する）。

---

## 📚 Git運用ルール

- **画面ごとに1コミット**（i18n作業なら「1画面 翻訳完了 → コミット」）
- メッセージ形式：`i18n: ○○Screen 多言語化完了`
- プレフィックス：`i18n:` / `fix:` / `feat:` / `docs:` / `refactor:`
- 作業前に `git status` でクリーンなことを確認
- Android実機で動作確認してからコミット

```bash
git status
git add -A
git commit -m "i18n: SettingsScreen 多言語化完了"
git push
```

---

## 🔲 次にやること（優先順）

1. ~~`SettingsScreen.js` の多言語化~~ ✅ **完了**（言語切替セクションも実装）
2. **`RecordDetail.js` の多言語化**（`CATEGORY_LABELS` 参照を `t('cat_'+...)` に置換）
3. **`ManualInput.js` の多言語化**（同上）
4. ~~`CsvExportModal.js` の多言語化~~ ✅ **完了**（下記「CSV出力の言語方針」を参照）
5. **`SettingsScreen_guide.js` の多言語化**（分量大）
6. **`App.js` のヘッダータイトル多言語化**（`'📸 スクショ取込'` 等がハードコード）
7. 技術的負債の整理（refactorコミット）
8. 以降は v7 の残タスクへ：MBジェム色の精度向上 / ダークテーマ / 文字サイズ / CoinGecko円換算

---

## 📖 過去ドキュメントの参照先

全11本のコンテキストを1本に統合したアーカイブがリポジトリ内にある。

**[`docs/handoff/STEPN_全コンテキスト統合アーカイブ_20260814.md`](./docs/handoff/STEPN_全コンテキスト統合アーカイブ_20260814.md)**（2,512行）

| # | 内容 | 作成日 |
|---|---|---|
| 1 | 設計メモ（最初期・Vision API前提の**旧方針**） | 初期 |
| 2 | 2026.05.03（過去チャットURL） | 05/03 |
| 3 | STEPN WHITEPAPER URL一覧（全41ページ） | ― |
| 4 | 引き継ぎ v2（21カテゴリ・Visionプロンプト時代） | 05/04 |
| 5 | 引き継ぎ v3（ML Kit移行・22カテゴリ・WP知識まとめ） | 05/06 |
| 6 | カテゴリ定義カタログ | 05/08 |
| 7 | 引き継ぎ v4（23カテゴリ・ハレーション防止ルール制定） | 05/11 |
| 8 | 引き継ぎ v6（6/16版・**v6最終**・`MANUAL_GROUPS` 収録） | 06/16 |
| 9 | 引き継ぎ v7（FNV-1a・MB自動検出・サムネイル） | 06/25 |
| 10-11 | 引き継ぎ v6（Drive版・6/14の旧版／11は10と完全同一） | 06/14 |

その他の資料：
- [`docs/reference/STEPN_schema_design_20260503.md`](./docs/reference/STEPN_schema_design_20260503.md) — 最初期のJSONスキーマ設計（18種のscreen_type時代）

> **⚠️ [1]〜[4] は Claude Vision API 前提の旧方針。現在は ML Kit OCR 一択（従量課金API絶対NG）に変更済みなので、技術方針として参照してはいけない。**

### 読む順の推奨

```
最新の正しい仕様が知りたい   → 本ドキュメント（v9）
カテゴリ体系の詳細           → 本ドキュメント「カテゴリ体系」章
STEPNのゲーム仕様            → アーカイブ [5] v3 末尾「ホワイトペーパー重要知識まとめ」
なぜこの設計になったのか      → アーカイブを [1]→[4]→[5]→[7]→[10]→[8]→[9] の順で時系列に読む
```

---

## 📚 参考URL

- STEPNホワイトペーパー：https://whitepaper.stepn.com/
- Success Rate Increment：https://whitepaper.stepn.com/earning-module/success-rate-increment-system
- チェーンカラー参考：https://www.pantone.com/articles/case-studies/stepn-pantone-partnership-colors
- Expo File System (SDK54)：https://docs.expo.dev/versions/v54.0.0/sdk/filesystem/
